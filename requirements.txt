# Pipeline de données
pandas>=2.0
numpy>=1.24

# Génération LLM (backend "ollama" : aucune dépendance supplémentaire, il
# utilise urllib, présent dans la bibliothèque standard de Python)

# Dashboard
streamlit>=1.35

# Export du rapport
python-docx>=1.1
reportlab>=4.0

# Tests
pytest>=8.0

# Variables d'environnement
python-dotenv>=1.0

# NB : le backend "dataiku" utilise le package `dataiku`, disponible
# nativement dans l'environnement d'exécution Dataiku (recipes/notebooks) ;
# il n'est pas listé ici car il ne s'installe pas via pip hors Dataiku.
